<Page x:Class="Lumen.App.Views.StartupManagerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Startup Manager">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Startup Application &amp; Task Manager" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="{Binding StatusText}" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,12"/>
            <WrapPanel Orientation="Horizontal">
                <ui:Button Content="Refresh List" Command="{Binding LoadEntriesCommand}" Margin="0,0,8,8" Padding="14,8">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ArrowSync24" />
                    </ui:Button.Icon>
                </ui:Button>
                <ui:Button Content="Disable All Low-Risk Items" Command="{Binding DisableAllSafeCommand}" Appearance="Primary" Margin="0,0,0,8" Padding="14,8">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="DismissCircle24" />
                    </ui:Button.Icon>
                </ui:Button>
            </WrapPanel>
        </StackPanel>

        <!-- DataGrid -->
        <ui:DataGrid Grid.Row="1" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
            <ui:DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200" FontSize="13" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Publisher / Path" Binding="{Binding PublisherOrPath}" Width="*" FontSize="12"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="150" FontSize="12"/>
                <DataGridTextColumn Header="Risk Level" Binding="{Binding Risk}" Width="100" FontSize="12"/>
                <DataGridTemplateColumn Header="Action" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:ToggleSwitch IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                             Command="{Binding DataContext.ToggleEntryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                             CommandParameter="{Binding}"
                                             OnContent="On" OffContent="Off"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </Grid>
</Page>
